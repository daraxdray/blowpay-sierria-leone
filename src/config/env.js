/**
 * App config. Change BASE_URL per environment (dev/staging/production).
 * This file is the single place for the API base URL instead of hardcoding in api.js.
 */
export const BASE_URL = 'https://api.blowcloud.org/v1';
